#!/bin/bash

NAMES="(Print working directory)"
for NAME in $NAMES
     do
        echo "se utiliza para imprimir el nombre del directorio actual en una sesión de comandos bajo un sistema operativo Unix $NAME"
     done
